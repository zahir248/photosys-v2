// Helper for MongoDB-compatible Firestore
// This would require a backend API since web apps can't directly use MongoDB-compatible Firestore

// For now, we'll continue using localStorage
// To use MongoDB-compatible Firestore, you'd need to create a Firebase Function backend

console.log('MongoDB helper loaded - using localStorage for now');

export {};

